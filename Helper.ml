open ProtocolDefinitions

(** val init_upload_connection :
    file_descr -> string -> string -> string -> sockaddr ->
    coq_PCState * coq_POutput **)

let init_upload_connection =
  failwith "AXIOM TO BE REALIZED"

(** val init_download_connection :
    file_descr -> string -> string -> string -> sockaddr ->
    coq_PCState * coq_POutput **)

let init_download_connection =
  failwith "AXIOM TO BE REALIZED"

(** val communication_timeout :
    coq_CProtoData -> coq_PCState * coq_POutput **)

let communication_timeout =
  failwith "AXIOM TO BE REALIZED"
